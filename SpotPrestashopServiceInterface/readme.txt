[README FIRST]
inside your prestashop folder create a new folder called service. Put all the php files in this folder.
If desired you can edit the config.php to set the image base path and the API key.

All request have to have the header "Api-Key" set with the API key from the config.php file for authentication.
